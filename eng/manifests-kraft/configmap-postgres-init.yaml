apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init-sql
  namespace: default
data:
  init.sql: |
    CREATE DATABASE messaging_outbox;
    CREATE DATABASE feed_ingestion;
    CREATE DATABASE ledger;
    CREATE DATABASE iceberg_catalog;
